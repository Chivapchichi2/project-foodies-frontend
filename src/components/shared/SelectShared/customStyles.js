const customStyles = {
  control: styles => ({
    ...styles,
    padding: '14px',
    borderRadius: '30px',
    border: '1px solid rgba(5, 5, 5, 0.12)',
  }),
  menu: styles => ({
    ...styles,
    padding: '16px 18px',
    borderRadius: '15px',
    border: '1px solid rgba(5, 5, 5, 0.20)',
  }),
};

export default customStyles;
